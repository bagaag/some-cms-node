var app = app || {};

$(function( $ ) {
    'use strict';

	// The admin dashboard hero area
	// ---------------

	app.HeroView = Backbone.View.extend({
        el: '#hero',
        initialize: function() {
            $('')    
        },
    });
    
});