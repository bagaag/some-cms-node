var app = app || {};

$(function( $ ) {
    'use strict';

	// The admin dashboard 
	// -------------------

	app.AppView = Backbone.View.extend({

        initialize: function() {
        },

    });
    
});
